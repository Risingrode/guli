spring:
  cloud:
    gateway:
      routes:
        - id: test_route
          uri: https://www.baidu.com
          predicates:
            - Query=url,baidu
        - id: qq_route
          uri: https://www.qq.com
          predicates:
<<<<<<< HEAD
            - Query=url,qq
=======
            - Query=url,qq
        - id: product_route # 路由id
          uri: lb://gulimall-product # 负载均衡
          predicates:
            - Path=/api/product/** # 匹配路径
          filters:
            - RewritePath=/product/(?<segment>.*), /$\{segment} # 重写路径
        - id: admin_route
          uri: lb://renren-fast
          predicates:
            - Path=/api/**
          filters:
            - RewritePath=/api/(?<segment>.*), /renren-fast/$\{segment}
    starter:
      loadbalancer:
        enabled: true
## 前端发送请求都带上api前缀
>>>>>>> dev
